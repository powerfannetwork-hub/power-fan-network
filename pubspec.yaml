name: powerfannetwork
description: "Power Fan Network App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  supabase_flutter: 2.15.4
  provider: ^6.1.2
  flutter_local_notifications: ^17.2.2
  http: ^1.2.2
  applovin_max: ^4.6.4
  package_info_plus: ^8.0.2
  url_launcher: ^6.3.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

dependency_overrides:
  app_links: 6.3.0

flutter:
  uses-material-design: true
