name: power_fan_network
description: POWER FAN NETWORK - AFAM
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # ==========================================================
  # HTTP
  # Custom Backend API communication
  # ==========================================================
  http: ^1.2.2

  # ==========================================================
  # LOCAL SESSION STORAGE
  # Stores Custom JWT token locally
  # ==========================================================
  shared_preferences: ^2.3.5

  # ==========================================================
  # UI ICONS
  # ==========================================================
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
