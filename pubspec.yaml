name: power_fan_network
description: POWER FAN NETWORK - Real FAN Mining Network
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1
  firebase_database: ^12.0.1
  firebase_messaging: ^16.0.0
  firebase_app_check: ^0.4.0+1

  # Authentication
  google_sign_in: ^7.1.1

  # Local storage
  shared_preferences: ^2.5.3

  # Device/security
  device_info_plus: ^11.5.0

  # UI
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
