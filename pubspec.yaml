name: power_fan_network
description: POWER FAN NETWORK - AFAM
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ==========================================================
  # SUPABASE
  # Authentication + Database + Edge Functions
  # ==========================================================
  supabase_flutter: ^2.17.2

  # ==========================================================
  # HTTP
  # ==========================================================
  http: ^1.2.2

  # ==========================================================
  # STATE MANAGEMENT
  # ==========================================================
  provider: ^6.1.5+1

  # ==========================================================
  # NAVIGATION
  # ==========================================================
  go_router: ^16.2.0

  # ==========================================================
  # LOCAL STORAGE
  # ==========================================================
  shared_preferences: ^2.3.5

  # ==========================================================
  # DEVICE INFORMATION
  # ==========================================================
  device_info_plus: ^12.1.0

  # ==========================================================
  # UNIQUE IDENTIFIERS
  # ==========================================================
  uuid: ^4.5.1

  # ==========================================================
  # URL / OFFICIAL SOCIAL MEDIA LINKS
  # ==========================================================
  url_launcher: ^6.3.2

  # ==========================================================
  # DATE / TIME
  # ==========================================================
  intl: ^0.20.2

  # ==========================================================
  # ICONS
  # ==========================================================
  font_awesome_flutter: ^10.9.0

  # ==========================================================
  # LOCAL NOTIFICATIONS
  # ==========================================================
  flutter_local_notifications: ^19.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
